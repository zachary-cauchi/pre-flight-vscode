# The API version of the preflight.
version: 0.0.1

# the name of the preflight.
name: Test pre-flight

# A list of procedures to be executed.
config:
  - name: test step
    files: package.json
    phrase: Requires a name be specified for the project
  - name: second test step
    files: tslint.ts
    phrase: DefaultSeverity should equal "warning"
